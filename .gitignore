*.zip
*.tgz
claude-linux-x64
Claude.app/
npm-package/package/
desktop/extracted/
desktop/*.zip
desktop/Claude.app/

# Binaries (too large for git — download via make fetch)
binaries/v*-*/claude
binaries/v*-*/claude.exe
binaries/claude-linux-*
# Pipeline-emitted manifest files in binaries/ are not tracked (binaries are regenerated)
binaries/v*-*/manifest.json

# bun-demincer is a separate repo; clone it into pocs/bun-demincer manually.
# See README + docs/exec-plans for source. Pipeline writes work/ inside it.
pocs/bun-demincer/

# Archive decoded modules (too large, regenerated by pipeline)
archive/*/modules/

# Monitor state
monitor/state/
.tmp-analysis-prompt.md

# Node
node_modules/
site/node_modules/
site/.astro/
bun.lockb
opencode-auth-research/*/node_modules/
opencode-auth-research/*/dist/

# OS
.DS_Store

# Pi session state (per-machine)
.pi/

# Excalidraw scratch
excalidraw.log

# Request-shape capture artifacts (need redaction before commit)
monitor/request-shape/artifacts/

# Decompiled CLI (too large, keep in binaries/)
pocs/api-intercept/decompiled/
pocs/api-intercept/captures/
