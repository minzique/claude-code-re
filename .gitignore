*.zip
*.tgz
claude-linux-x64
Claude.app/
npm-package/package/
desktop/extracted/

# Binaries (too large for git — download via make fetch)
binaries/v*-*/claude
binaries/v*-*/claude.exe
binaries/claude-linux-*

# bun-demincer working dirs (regenerated by pipeline)
pocs/bun-demincer/work/
pocs/bun-demincer/node_modules/

# Archive decoded modules (too large, regenerated by pipeline)
archive/*/modules/

# Monitor state
monitor/state/
.tmp-analysis-prompt.md

# Node
node_modules/
site/node_modules/
site/.astro/
bun.lockb

# OS
.DS_Store

# Decompiled CLI (too large, keep in binaries/)
pocs/api-intercept/decompiled/
pocs/api-intercept/captures/
